{
  "name": "rol.church",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/check-assets.mjs",
    "build:only": "astro build",
    "check-assets": "node scripts/check-assets.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.0"
  }
}
