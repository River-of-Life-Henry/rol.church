---
import Base from '../layouts/Base.astro';
import HeroSlider from '../components/HeroSlider.astro';
---

<Base>
  <HeroSlider>
    <div class="hero-cards">
      <a href="/next-steps/visit" class="hero-card">
        <h2>PLAN YOUR VISIT</h2>
        <p>We'd love to meet you! Plan your visit and let us help make your first experience welcoming and memorable.</p>
        <span class="card-btn">Plan Your Visit</span>
      </a>
      <a href="https://rolhenry.churchcenter.com/live" target="_blank" class="hero-card">
        <h2>WATCH LIVE</h2>
        <p>Can't make it in person? Join us online for our live stream every Sunday at 10:00 AM.</p>
        <span class="card-btn">Watch Now</span>
      </a>
    </div>
  </HeroSlider>

  <section class="service-info">
    <div class="container">
      <div class="service-block">
        <span class="label">Sunday</span>
        <span class="time">10am & 11am</span>
      </div>
      <div class="divider"></div>
      <div class="service-block">
        <span class="label">Wednesday</span>
        <span class="time">7:00 PM</span>
      </div>
      <div class="divider"></div>
      <div class="service-block">
        <span class="label">Location</span>
        <span class="location">425 University Ave, Henry IL</span>
      </div>
    </div>
  </section>

  <section class="about-section">
    <div class="container">
      <h2>Where life begins</h2>
      <p>ROL is a loving community of Christ-followers dedicated to serving people on their spiritual journey. Whether you're exploring faith for the first time or have been walking with God for years, there's a place for you here.</p>
      <a href="/about" class="text-link">Learn more about us &rarr;</a>
    </div>
  </section>

  </Base>

<style>
  /* Hero cards */
  .hero-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 900px;
    width: 100%;
    padding: 0 2rem;
  }

  .hero-card {
    background: rgba(45, 156, 202, 0.75);
    backdrop-filter: blur(10px);
    border-radius: 1rem;
    padding: 2.5rem;
    text-decoration: none;
    color: white;
    display: flex;
    flex-direction: column;
    min-height: 280px;
    transition: transform 0.3s, background 0.3s;
  }

  .hero-card:hover {
    transform: translateY(-4px);
    background: rgba(45, 156, 202, 0.85);
    color: white;
  }

  .hero-card h2 {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }

  .hero-card p {
    font-size: 0.95rem;
    line-height: 1.6;
    opacity: 0.9;
    flex: 1;
  }

  .card-btn {
    display: inline-block;
    background: white;
    color: var(--color-navy);
    padding: 0.875rem 2rem;
    border-radius: 2rem;
    font-weight: 600;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 1.5rem;
    text-align: center;
    transition: background 0.2s;
  }

  .hero-card:hover .card-btn {
    background: var(--color-gold);
  }

  /* Service info bar */
  .service-info {
    background: var(--color-navy);
    color: white;
    padding: 1.5rem 2rem;
  }

  .service-info .container {
    max-width: 600px;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2rem;
  }

  .service-block {
    text-align: center;
  }

  .service-block .label {
    display: block;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.7;
    margin-bottom: 0.25rem;
  }

  .service-block .time,
  .service-block .location,
  .service-block .phone {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-gold);
    text-decoration: none;
  }

  .service-block .phone:hover {
    color: white;
  }

  .divider {
    width: 1px;
    height: 40px;
    background: rgba(255,255,255,0.2);
  }

  /* About section */
  .about-section {
    padding: 6rem 2rem;
    text-align: center;
  }

  .about-section .container {
    max-width: 600px;
    margin: 0 auto;
  }

  .about-section h2 {
    font-size: 2.5rem;
    font-weight: 400;
    color: var(--color-navy);
    margin-bottom: 1.5rem;
  }

  .about-section p {
    font-size: 1.125rem;
    line-height: 1.8;
    color: var(--color-text-light);
    margin-bottom: 2rem;
  }

  .text-link {
    font-weight: 500;
    color: var(--color-teal);
    text-decoration: none;
    transition: color 0.2s;
  }

  .text-link:hover {
    color: var(--color-teal-dark);
  }

  @media (max-width: 768px) {
    .hero-cards {
      grid-template-columns: 1fr;
      gap: 1rem;
      padding: 0 1rem;
    }
    .hero-card {
      min-height: auto;
      padding: 1.5rem;
    }
    .hero-card h2 {
      font-size: 1.25rem;
    }
    .service-info .container {
      flex-direction: column;
      gap: 1rem;
    }
    .divider {
      width: 40px;
      height: 1px;
    }
    .about-section h2 {
      font-size: 2rem;
    }
  }
</style>
