---
/**
 * HeroCards Component
 *
 * "Plan Your Visit" and "Watch Live" cards displayed in the hero slider.
 *
 * Usage:
 *   <HeroCards />
 */
---

<div class="hero-cards">
  <a href="/next-steps/visit" class="hero-card">
    <h2>PLAN YOUR VISIT</h2>
    <p class="subtitle">We'd love to meet you!</p>
    <p>Plan your visit and let us help make your first experience welcoming.</p>
    <span class="card-btn">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10zM5 8V6h14v2H5zm2 4h10v2H7v-2zm0 4h7v2H7v-2z"/></svg>
      Plan Now
    </span>
  </a>
  <a href="/live" class="hero-card">
    <h2><span class="home-live-dot" style="display: none;"></span>WATCH LIVE</h2>
    <p class="subtitle">Can't make it in person?</p>
    <p>Join us every Sunday and Wednesday for our live stream.</p>
    <span class="card-btn">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
      Watch Now
    </span>
  </a>
</div>

<style>
  .hero-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 900px;
    width: 100%;
    padding: 0 2rem;
  }

  .hero-card {
    background: rgba(45, 156, 202, 0.65);
    backdrop-filter: blur(10px);
    border-radius: 1rem;
    padding: 2.5rem;
    text-decoration: none;
    color: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    min-height: 280px;
    transition: transform 0.3s, background 0.3s;
  }

  .hero-card:hover {
    transform: translateY(-4px);
    background: rgba(45, 156, 202, 0.75);
    color: white;
  }

  .hero-card h2 {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .home-live-dot {
    width: 10px;
    height: 10px;
    background-color: #ef4444;
    border-radius: 50%;
    animation: live-pulse 2s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes live-pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .hero-card p {
    font-size: 0.95rem;
    line-height: 1.6;
    opacity: 0.9;
    flex: 1;
  }

  .hero-card .subtitle {
    font-size: 1.1rem;
    font-weight: 600;
    opacity: 1;
    flex: none;
    margin-bottom: 0.5rem;
  }

  .card-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: white;
    color: var(--color-navy);
    padding: 0.875rem 2rem;
    border-radius: 2rem;
    font-weight: 600;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 1.5rem;
    text-align: center;
    transition: background 0.2s;
  }

  .card-btn svg {
    width: 1.125em;
    height: 1.125em;
  }

  .hero-card:hover .card-btn {
    background: var(--color-gold);
  }

  @media (max-width: 768px) {
    .hero-cards {
      grid-template-columns: 1fr;
      gap: 3rem;
      max-width: 320px;
      padding: 0 1rem;
      margin: 0 auto;
    }
    .hero-card {
      min-height: auto;
      padding: 1rem 1.25rem;
    }
    .hero-card h2 {
      font-size: 0.9rem;
      margin-bottom: 0.4rem;
      letter-spacing: 0.08em;
    }
    .hero-card p {
      font-size: 0.8rem;
      line-height: 1.45;
    }
    .hero-card .subtitle {
      font-size: 0.8rem;
      margin-bottom: 0.3rem;
    }
    .card-btn {
      padding: 0.6rem 1.25rem;
      font-size: 0.75rem;
      margin-top: 0.65rem;
    }
    .card-btn svg {
      width: 15px;
      height: 15px;
    }
  }
</style>
